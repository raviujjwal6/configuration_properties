spring:
  application:
    name: school-service

  datasource:
    url: jdbc:mysql://localhost:3306/DB_NAME
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

    show-sql: true
